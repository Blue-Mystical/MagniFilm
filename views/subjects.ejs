<h2>Subject list</h2>

<% subjects.forEach(function(subject) { %>
    <li> <%= subject %> </li>
<% }); %>

<form action = '/new' method = 'POST'>
    <input type='text' name='newsubject' placeholder='Subject name'>
    <button>Add new subject</button>
</form>